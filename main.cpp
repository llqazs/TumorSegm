#include <iostream>

using namespace std;

int main()
{
    //To do: two applications
    //1) practical use, provide user input-----------------------------------------------
    //graphical user interface for crop image, click seeds

    //1.1)get user input, bounding box, seeds and options
    //1.2)algorithm set up
    //  1.2.1)data set up: fgseeds, bkseeds, roi, box_fg, star shape centers
    //  1.2.2)configuraton set up: energy term options, bin number, min area, max area, show or not ...

    //2)begin to segment and show the result

    //2) for experiment use, load cropped image and simulated user input-------------------
    //load image, user input, segment

    //load gt, compute statistics


    return 0;
}
